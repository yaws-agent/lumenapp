node_modules/
*.log
.DS_Store
dist/
release/

# Images / videos / docs stay local (never commit)
*.png
*.mp4
*.mov
*.pdf
AGENT_TASK.md
BRIEFING.md

# Credential files (never commit)
.hermes/
ghpat

# Exception: track the app icon source so the build is reproducible
!build/icon.png
